<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.back.mapper.DealreviewMapper">
    <!-- 받은 거래 후기 -->
    <select id="dealReview" parameterType="String" resultType="com.sist.back.vo.PostVO">
        SELECT p.userreview, p.userreviewimg, p.dealuserreview, p.dealuserreviewimg
        FROM post p
        JOIN user u ON u.userkey = p.dealuserkey
        WHERE u.userkey = #{userkey};
    </select>
</mapper>